# DBO2

Small Python project — files: `data.py`, `database.py`, `main.py`.

## Local setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt  # if you add one
```

## To put this on GitHub

- I can create the remote repo and push for you (requires GitHub auth), or
- You can create an empty repo on GitHub and give me the remote URL to push to.
